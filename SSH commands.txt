#you can also use winSCP .. connect your drone and 
#copy "channels.sh" into "/usr/bin "
#and copy/replace "rc.local" to "/etc/"

#run these commands after movine files 

chmod +x /usr/bin/channels.sh

chmod +x /etc/rc.local